---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class={`bg-white rounded-xl shadow-lg border border-gray-100 overflow-hidden ${className}`}>
  <slot />
</div>
